<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.studentfeedback">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MatTheme">
        <activity android:name=".dFragment"></activity>
        <activity android:name=".TabActivity" />
        <activity android:name=".underConstruction" />
        <activity android:name=".options" />
        <activity android:name=".IndividualComment" />
        <activity android:name=".AllComments" />
        <activity android:name=".CourseSurvey" />
        <activity android:name=".CourseDetail" />
        <activity android:name=".SelectUniversity" />
        <activity android:name=".SignupPage" />
        <activity android:name=".CourseSearch" />
        <activity android:name=".Login" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>